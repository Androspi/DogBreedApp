import { BreedDirective } from './breed.directive';

describe('BreedDirective', () => {
  it('should create an instance', () => {
    // const directive = new BreedDirective();
    // expect(directive).toBeTruthy();
  });
});
